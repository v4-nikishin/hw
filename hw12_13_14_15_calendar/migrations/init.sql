CREATE table events (
    id              serial primary key,
    uuid            text,
    title           text,
    user            tetx,
    date            tetx,
    begin           text,
    end             tetx
);
